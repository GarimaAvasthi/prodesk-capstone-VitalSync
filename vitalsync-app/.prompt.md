# Prompts Used During VitalSync Development

A curated collection of the key AI prompts used while building VitalSync. All these prompts were used only for guiding and suggestions , all the work has been manually done.
---

## Prompts

- How to Use a sidebar component that renders navigation links filtered by the current user's role.
- Reduce time of all toast notifications
- Integrate a Google Gemini-powered AI chatbot into a Next.js healthcare dashboard. The chatbot should act as a medical workspace assistant — not a general-purpose bot.
- Handle API failures gracefully — show an inline error message in the chat, never crash the UI.
- The chatbot must be globally accessible across all dashboard pages (Patient, Doctor, Admin), not mounted per-page.
- Replace the desktop sidebar with a hamburger menu (☰) that triggers a slide-in drawer overlay. The drawer must include all role-filtered nav links + logout button + user info.
-  Store the authentication role in Zustand and persist to a cookie named 'auth-session'.
- map every Firebase error code to a specific, user-friendly message. Do NOT show raw Firebase error strings:
- Help with next.js middleware that protects all authenticated routes using cookie-based session detection. The middleware must:
- Build a marketing landing page for VitalSync at the root route (/) using Framer Motion for scroll-triggered animations.
- Optimize my entire website to make it more organized
- What kind of security does a Healthcare application demands in terms of safety and security of user's pesonal data
- Best steps  to update my performance score in chromium dev tools